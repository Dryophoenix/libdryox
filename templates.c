/*
template is a libdryox module.
It serves the following functions:
    example(void);

It serves the following types:
    typedef enum example;

It accepts the following environment variables:
    HOME;
*/
